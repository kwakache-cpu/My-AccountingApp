# PostgreSQL Migration Scorecard (Phase 5B.11)

**Audited at:** 2026-06-02 12:04:36 UTC

| Dimension | Grade | Notes |
|-----------|-------|-------|
| Startup guardrail | **YELLOW** | Phase 5B.13B blocks enabled PostgreSQL runtime before SQLite-only schema/recovery paths; schema deployment still not implemented |
| Schema deployment plan | **YELLOW** | Phase 5B.13C design plan exists in `reports/postgres_schema_deployment_plan.md`; deployer has not been implemented |
| Schema introspection abstraction | **YELLOW** | Phase 5B.13D adds backend-aware table/column/index/FK helpers and converts low-risk diagnostics/readiness callers; schema deployment paths remain SQLite-native |
| DDL generator | **YELLOW** | Phase 5B.13E adds offline `postgres_schema_generator.py` plus generated SQL/summary artifacts; no schema deployment or Supabase connection |
| Schema validator | **YELLOW** | Phase 5B.13F adds offline `postgres_schema_validator.py` and validation report; score 90/100 with 51/51 tables, 67 indexes, 47 FKs, and no forbidden SQLite syntax in generated SQL |
| Staging deployment skeleton | **YELLOW** | Phase 5B.13H adds `postgres_staging_deployer.py` with dry-run artifact checks, redacted DATABASE_URL diagnostics, and blocked `--apply`; execution is not implemented |
| Post-deployment validation framework | **YELLOW** | Phase 5B.13I adds offline `postgres_postdeploy_validator.py` and a validation plan covering schema/table/column/index/FK/seed/migration/runtime readiness checks; no DB access or execution |
| Deployment executor design | **YELLOW** | Phase 5B.14A adds `reports/postgres_deployment_executor_design.md` covering executor architecture, phase execution, transactions, rollback, migration history, validation checkpoints, logging, and safety protections; implementation has not started |
| Migration history framework | **YELLOW** | Phase 5B.14C adds offline `postgres_migration_history.py`, migration run/event table designs, dry-run phase events, and executor dry-run history objects; PostgreSQL writes remain unimplemented |
| Validation query framework | **YELLOW** | Phase 5B.14D adds offline `postgres_validation_queries.py` with PostgreSQL-oriented validation query definitions for schema/table/column/index/FK/seed/history/runtime checks; execution remains unimplemented |
| Connection adapter skeleton | **YELLOW** | Phase 5B.14E adds `postgres_connection_adapter.py` with redacted DATABASE_URL diagnostics, safe URL parsing, and driver discovery only; connection execution remains unimplemented |
| Controlled connection probe framework | **YELLOW** | Phase 5B.14G adds timeout-aware `postgres_staging_deployer.py --probe` execution; it requires `ERP_ENABLE_POSTGRES_PROBE=1` and `DATABASE_URL`, can only connect/disconnect once, redacts credentials, and never deploys schema, runs migrations, executes SQL, imports Supabase, or enables runtime |
| Schema execution adapter | **YELLOW** | Phase 5B.14H adds `postgres_schema_executor.py` for dry-run parsing/planning of `reports/postgres_generated_schema.sql` plus unit-test-only mock execution and rollback modeling; CLI `--apply` remains blocked and no real connection, DATABASE_URL, Supabase, migration, or runtime path is used |
| Guarded staging schema apply | **YELLOW** | Phase 5B.14I adds `SchemaApplyGuard` and guarded `--apply --confirm-schema-apply` diagnostics requiring `ERP_ENABLE_POSTGRES_SCHEMA_APPLY=1`, `ERP_ENVIRONMENT=staging`, `DATABASE_URL`, explicit apply, and confirmation; status remains BLOCKED and no SQL/schema/migration/runtime path executes |
| Schema execution audit framework | **YELLOW** | Phase 5B.14J adds `SchemaExecutionAuditEvent`, `SchemaExecutionAuditLog`, and `SchemaExecutionAuditStatus`; dry-run creates PLANNED audit events and blocked apply creates a BLOCKED audit event, with no SQL, connection, Supabase, schema deployment, migration, or runtime path |
| Mock schema execution engine | **YELLOW** | Phase 5B.14K adds `execute_schema_plan_with_connection()` for injected mock connections only; it supports ordered statement execution, commit-on-success, rollback-on-failure, and RUNNING/COMPLETED/FAILED/ROLLED_BACK audit events, while CLI `--apply` remains blocked and no real connection, Supabase, runtime, or migration path exists |
| Real guarded staging schema apply | **YELLOW** | Phase 5B.14L adds real staging-only schema apply behind `ERP_ENABLE_POSTGRES_SCHEMA_APPLY=1`, `ERP_ENVIRONMENT=staging`, `ERP_ENABLE_POSTGRES_PROBE=1`, `DATABASE_URL`, `--apply`, `--confirm-schema-apply`, and a successful safe probe; data migration, seed data, runtime activation, SQLite changes, and production deployment remain blocked |
| Dependency-ordered schema generation | **YELLOW** | Phase 5B.14N updates `postgres_schema_generator.py` to topologically order table DDL from FK metadata; regenerated SQL preserves 51 tables and 47 FK edges, creates `companies` before dependent tables, reports 0 dependency cycles, and keeps apply/runtime/data migration controls unchanged |
| Post-deployment validation execution | **YELLOW** | Phase 5B.15E reran guarded staging-only read-only validation successfully: 754/754 checks passed, 0 failed, guards passed, and runtime/data migration/production deployment/SQLite behavior stayed unchanged |
| Data migration framework | **YELLOW** | Phase 5B.15I reconciles the completed staging row copy with read-only row-count checks across 51 tables: 527 SQLite rows matched 527 PostgreSQL rows, 51/51 tables matched, 0 mismatches, 0 missing rows, and 0 extra rows; status is READY_FOR_RUNTIME_VALIDATION while runtime and production remain blocked |
| Runtime readiness validation | **GREEN** | Phase 5B.15J performs read-only PostgreSQL runtime readiness validation against staging: 51/51 runtime tables passed, 0 executable indexes were required by the generated schema artifact, 47/47 FK checks passed, 6/6 smoke counts passed, 0 blockers were found, and status is READY_FOR_RUNTIME_CUTOVER while runtime activation, SQLite modification, application writes, and production deployment remain blocked |
| Runtime cutover plan | **GREEN** | Phase 5B.15K documents the final runtime cutover and rollback design, including pre-cutover backup, write freeze, final SQLite snapshot, final reconciliation, Streamlit secrets, deploy, smoke testing, monitoring, rollback decision window, rollback steps, and NO-GO criteria; this is plan-only and does not enable PostgreSQL runtime or deploy production |
| Runtime dry-run validation | **GREEN** | Phase 5B.15L adds guarded PostgreSQL runtime dry-run validation behind `ERP_ENVIRONMENT=staging`, `ERP_ENABLE_POSTGRES_RUNTIME_DRYRUN=1`, and `DATABASE_URL`; startup, business-module, and reporting read paths passed 13/13 SELECT-only checks with 0 failures and 0 blockers, producing READY_FOR_RUNTIME_CUTOVER without permanent runtime enablement, SQLite data modification, application writes, migrations, or production deployment |
| Controlled runtime cutover | **GREEN** | Phase 5B.15M adds a final runtime cutover guard/check for `DB_BACKEND=postgres`, `ERP_ENABLE_POSTGRES_RUNTIME=1`, `DATABASE_URL`, and staging or production-approved environment, surfaces redacted startup diagnostics plus schema deployment and row reconciliation status, verifies SQLite-only startup paths remain blocked when PostgreSQL runtime is selected, and records the execution checklist and rollback steps without committing, pushing, modifying SQLite data, deleting backups, or deploying production |
| Runtime smoke test | **GREEN** | Phase 5B.15N runs controlled staging/local PostgreSQL runtime activation smoke validation with `DB_BACKEND=postgres`, `ERP_ENABLE_POSTGRES_RUNTIME=1`, `ERP_ENVIRONMENT=staging`, and `DATABASE_URL`; active backend resolved to postgres, SQLite bootstrap stayed blocked, the PostgreSQL connection opened, companies/users/chart/customers/inventory/journals/dashboard counts passed 9/9 read-only checks, and Streamlit secrets cutover is READY while SQLite modification, backup deletion, commits, pushes, and production deployment remain blocked |
| PostgreSQL runtime startup path | **GREEN** | Phase 5B.15O enables `startup_database()` to proceed with `stage=postgres_runtime_startup` only after `DB_BACKEND=postgres`, `ERP_ENABLE_POSTGRES_RUNTIME=1`, `DATABASE_URL`, staging or approved production environment, and all required validation evidence pass; SQLite schema/bootstrap/recovery/migration paths remain blocked under PostgreSQL runtime, missing evidence still blocks, and the refreshed smoke test passes with active backend postgres and 9/9 read checks |
| Auth/subscription runtime portability | **GREEN** | Phase 5B.15P routes subscription-plan and subscription metadata reads through backend-aware connections and portable SELECT placeholders, adds a read-only auth/access-key lookup helper for company, branch, and staff login paths, verifies PostgreSQL runtime does not open SQLite for subscription-plan reads, and preserves SQLite behavior with focused tests |
| Runtime portability sweep | **GREEN** | Phase 5B.15P sweep fixes remaining high-priority PostgreSQL runtime read blockers for dashboard metric reads and system diagnostics, verifies login/access-key and subscription paths do not open SQLite, blocks SQLite-only recovery/restore while PostgreSQL runtime is active, and records remaining module/accounting/financials SQLite-native paths for staged portability follow-up |
| Runtime compatibility hardening | **GREEN** | Phase 5B.15Q adds central row compatibility helpers for dict, SQLite Row, PostgreSQL tuple/description rows, namedtuples, and object rows; wraps portable PostgreSQL SELECT results as key/index-compatible rows; hardens sidebar currency, recovery, session/company context, dashboard, branch, maintenance, system-status, admin portfolio, module currency/context, financial page-load, and chart/report settings reads; and records remaining write-heavy/page-level blockers for staged hardening |
| Runtime SQL dialect hardening | **GREEN** | Phase 5B.15R blocks SQLite-only runtime DDL (`AUTOINCREMENT`, `PRAGMA`, `sqlite_master` self-heal) under PostgreSQL, converts dashboard/inventory/POS page-load, chart-of-accounts, journal report, and system-log read paths to portable SELECT helpers, and records remaining POS search/checkout and write/posting blockers for staged validation |
| Runtime SQL hotfix 2 | **GREEN** | Phase 5B.15S fixes the remaining executive-dashboard inventory KPI queries and POS `get_customer_balances()` customer lookup that still used raw `conn.execute()` with SQLite `?` placeholders and unqualified `ORDER BY name`; dashboard analytics bundle reads and supplier/customer balance lookups now route through portable SELECT helpers |
| Row compatibility sweep | **GREEN** | Phase 5B.15T adds `fetch_scalar()`, bounds-safe `CompatibleRow` indexing, and converts remaining runtime `fetchone()[0]`, positional `row[n]`, and sqlite3.Row-only branches in database/modules/accounting_engine to `row_get()` / `row_to_dict()` / portable SELECT helpers; audit/period/inventory column probes use `list_columns()` |
| Runtime final blockers | **GREEN** | Phase 5B.15U fixes PostgreSQL `%` format errors in LIKE patterns, portable month filters, and customer balance reads that blocked POS and dashboard KPI paths |
| Runtime trace and report fixes | **GREEN** | Phase 5B.15V fixes dashboard aborted-transaction poisoning from optional POS reads, replaces `sqlite_master` source-document probes with `db_table_exists()`, adds PostgreSQL rollback-on-read-failure, and hardens Financial Reports integrity diagnostics |
| Identity portability | **GREEN** | Production paths use get_inserted_id / ensure_insert_sql_returning; zero raw lastrowid in app modules |
| Placeholder portability | **YELLOW** | High-priority page-load reads now route through portable helpers; write-heavy module/accounting paths still contain many literal `?` placeholders |
| Schema introspection portability | **YELLOW** | Shared helper layer exists, but many schema/self-heal and module callers still use SQLite-native PRAGMA/sqlite_master |
| Transaction portability | **YELLOW** | db_begin/commit OK; BEGIN IMMEDIATE SQLite-only in lock wrapper |
| Accounting portability | **YELLOW** | Journal identity GREEN; SQL dialect and strftime filters RED/YELLOW |
| POS portability | **YELLOW** | Sale identity converted; POS page-load DDL blocked on PostgreSQL and initial reads hardened; interactive search/checkout SQL dialect still needs staged validation |
| Inventory portability | **YELLOW** | Stock movement identity OK; inventory page-load reads hardened; write/schema probes outside page-load still SQLite-specific |
| Branch governance portability | **YELLOW** | Business logic present; introspection/SQL not Postgres-ready |
| Auth portability | **YELLOW** | Phase 5B.15Q keeps login/access-key, recovery lookup, session-lock, company context, and subscription reads on portable SELECT helpers with row-shape guard coverage; remaining auth write/reset/audit paths still need staged PostgreSQL runtime validation |
| Reporting portability | **YELLOW** | Phase 5B.15Q hardens chart-of-accounts diagnostics, system setting reads, financial read tables, and fixed-asset/journal DataFrame loads; journal posting/integrity SQL dialect and remaining accounting_engine schema probes still need page-level PostgreSQL validation |
| Data readiness | **GREEN** | FK orphans 0 on SQLite snapshot; cleanup phases documented |
| Overall staging readiness | **YELLOW** | Phase 5B.15V fixes PostgreSQL dashboard aborted-transaction errors and Financial Reports `UndefinedTable` from `sqlite_master` probes; staging runtime page testing can resume for Dashboard and Financial Reports after validation, while production deployment remains blocked until explicit approval and final deployment validation |

## Phase 5B.10 identity work (reflected)

- `post_journal_entry` — converted (5B.10G)
- POS, payments, bills, invoices, payroll, fixed assets, stock movements — `get_inserted_id` pattern
- Remaining `lastrowid` — tests, `database.py` helper implementation, audit scripts only

## Overall recommendation

**READY_TO_CONTINUE_STAGING_POSTGRES_RUNTIME_PAGE_TESTS** for Dashboard, Financial Reports, and prior validated paths after validation passes. Continue blocking production deployment until explicit approval and final deployment validation. Phase 5B.13B added a safe startup gate, Phase 5B.13C adds the schema deployment plan, Phase 5B.13D adds backend-aware introspection helpers, Phase 5B.13E adds offline DDL generation, Phase 5B.13F adds offline schema validation, Phase 5B.13H adds a staging deployment CLI skeleton, Phase 5B.13I adds a post-deployment validation framework, Phase 5B.14A adds the deployment executor design, Phase 5B.14C adds the migration history framework, Phase 5B.14D adds validation query definitions, Phase 5B.14E adds a blocked connection adapter skeleton, Phase 5B.14G adds an explicitly enabled, timeout-aware connection probe, Phase 5B.14H adds a dry-run schema execution adapter, Phase 5B.14I adds guarded staging apply diagnostics, Phase 5B.14J adds schema execution audit logging, Phase 5B.14K adds a mock-only execution engine, Phase 5B.14L adds real guarded staging schema apply, Phase 5B.14N fixes generated schema ordering, Phase 5B.14O adds guarded read-only post-deployment validation execution, Phase 5B.15D records the schema reconciliation plan for prior data-migration blockers, Phase 5B.15E revalidates 0 schema mismatches, 0 blockers, passing staging postdeploy checks, and 47/47 reconciled POS/cashier columns present in deployed PostgreSQL, Phase 5B.15F proves all 527 SQLite rows can be projected into the PostgreSQL schema with 0 mapping failures, Phase 5B.15G adds the mock-first row-copy engine structure with blocked-by-default execution, injected mock connection writes, success commit, and failure rollback coverage, Phase 5B.15H adds guarded real row-copy to staging with default BLOCKED behavior and explicit operator confirmation, Phase 5B.15I confirms post-copy reconciliation is READY_FOR_RUNTIME_VALIDATION with 51/51 tables matched and 0 missing or extra rows, Phase 5B.15J confirms runtime readiness with 51/51 tables, 47/47 FK checks, and 6/6 smoke checks passing, Phase 5B.15K documents the cutover, smoke-test, monitoring, NO-GO, and rollback plan, Phase 5B.15L confirms guarded runtime dry-run validation with startup, business, and reporting read paths passing 13/13 checks, Phase 5B.15M adds final cutover guard diagnostics plus the execution checklist, Phase 5B.15N confirms active backend postgres, SQLite bootstrap blocked, and 9/9 runtime smoke reads passing, Phase 5B.15O enables guarded PostgreSQL startup with `postgres_runtime_startup`, Phase 5B.15P fixes auth/subscription/dashboard/system-diagnostic runtime reads that still opened SQLite or used raw SQLite placeholders while blocking SQLite-only recovery under PostgreSQL runtime, Phase 5B.15Q hardens common post-login app/module/financial/report UI reads against PostgreSQL tuple row shape, Phase 5B.15R blocks SQLite-only runtime DDL under PostgreSQL and hardens common page-load SQL dialect paths for dashboard, inventory, POS, chart-of-accounts, and journal reads, Phase 5B.15S fixes the remaining executive-dashboard inventory KPI and POS customer lookup queries that still passed raw SQLite `?` placeholders to PostgreSQL, Phase 5B.15T eliminates remaining runtime `tuple index out of range` failures from positional row reads in database/modules/accounting_engine, Phase 5B.15U fixes PostgreSQL `%` format and portable month/customer balance reads for POS and dashboard KPI paths, and Phase 5B.15V fixes dashboard aborted-transaction poisoning and Financial Reports `sqlite_master` UndefinedTable failures. Production deployment remains blocked.
